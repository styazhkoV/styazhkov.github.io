---
const { title } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
import "../styles/global.css";
---

<html lang="ru">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>{title}</title>
    </head>
    <body class="bg-gray-50 text-slate-900 font-sans antialiased">
        <header class="bg-white border-b sticky top-0 z-50">
            <div
                class="max-w-6xl mx-auto px-4 py-6 flex justify-between items-center"
            >
                <a
                    href={baseUrl}
                    class="text-xl font-bold tracking-tight text-blue-600"
                >
                    STYAZHKOV.ANALYSIS
                </a>
                <nav class="space-x-6 text-sm font-medium">
                    <a
                        href={`${baseUrl}/#cases`}
                        class="text-slate-600 hover:text-blue-600 transition"
                        >Кейсы</a
                    >
                    <a
                        href="https://github.com/styazhkoV"
                        target="_blank"
                        class="hover:text-blue-600 transition">GitHub</a
                    >
                </nav>
            </div>
        </header>

        <slot />
        <footer
            class="max-w-6xl mx-auto px-4 py-12 border-t text-center text-slate-400 text-sm"
        >
            © {new Date().getFullYear()} Стяжков. Сделано с помощью Astro.
        </footer>
    </body>
</html>
