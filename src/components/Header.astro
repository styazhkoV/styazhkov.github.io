---
const { baseUrl, casesUrl, githubUrl } = Astro.props;
---

<header class="bg-white border-b sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-6 flex justify-between items-center">
        <a href={baseUrl} class="text-xl font-bold tracking-tight text-blue-600"
            >STYAZHKOV.ANALYSIS</a
        >
        <nav class="space-x-6 text-sm font-medium">
            <a
                href={casesUrl}
                class="text-slate-600 hover:text-blue-600 transition">Cases</a
            >
            <a
                href={githubUrl}
                target="_blank"
                class="hover:text-blue-600 transition">GitHub</a
            >
        </nav>
    </div>
</header>
